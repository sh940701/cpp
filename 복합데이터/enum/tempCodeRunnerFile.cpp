/ 아래와 같이 Color형의 변수를 선언하여, 그 안에 enum값을 넣을 수 있다.
    Color color = Color::Red;
    if (color == Color::Red){
            cout << "Red" << endl;
    }
    if (color == Color::Green){
            cout << "Green" << endl;
    }